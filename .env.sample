# MageFleet Global Configuration
# This file should be placed in ~/.magefleet/.env

# Traefik Configuration
# Updated to version 2.11 to support Docker API 1.44+
TRAEFIK_VERSION=2.11

# Set to "1" to enable global Portainer service
MAGEFLEET_PORTAINER_ENABLE=0

# Set to "0" to disable DNSMasq
MAGEFLEET_DNSMASQ_ENABLE=1

# Service Domain (default: magefleet.test)
MAGEFLEET_SERVICE_DOMAIN=magefleet.test

# Restart Policy for containers
MAGEFLEET_RESTART_POLICY=always

# Traefik Listen Address (default: 127.0.0.1)
# Set to 0.0.0.0 to expose on all interfaces (not recommended for security)
TRAEFIK_LISTEN=127.0.0.1

# Image Repository (default: docker.io/wardenenv)
# Change if you want to use custom images
MAGEFLEET_IMAGE_REPOSITORY=docker.io/wardenenv
